# Direct UPI payment demo

Static, fictional, non-custodial flow. It creates no UPI URI/QR, stores no UPI ID/UTR/screenshot, and moves no money.

Run `python3 -m http.server 4173` and open `/payments-demo/`.
