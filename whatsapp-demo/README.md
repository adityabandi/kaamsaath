# WhatsApp conversation demo

Static, interactive product prototype. It does not call Meta, send messages, store phone numbers, or accept terms.

From the repository root:

```bash
python3 -m http.server 4173
```

Open `http://localhost:4173/whatsapp-demo/`.
