# Trust operations and governance demo

Static interactive demonstration using fictional cases. No report, enforcement, external contact, or data collection occurs.

Run `python3 -m http.server 4173` from the repository root and open `/governance-demo/`.
