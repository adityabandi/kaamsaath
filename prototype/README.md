# Interactive worker demo

Run from the repository root:

```bash
python3 -m http.server 4173
```

Open `http://localhost:4173/prototype/`.

The prototype is static and deliberately has no backend, analytics, data collection, or external submission.
