# Cost-aware scoping demo

Static interactive prototype. It makes no model, speech, WhatsApp, analytics, or storage calls.

Run `python3 -m http.server 4173` from the repository root and open `/scoping-demo/`.
