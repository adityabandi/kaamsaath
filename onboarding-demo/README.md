# First-time onboarding demo

Static interactive demo for worker and hirer WhatsApp onboarding. It stores nothing and sends no messages.

Run `python3 -m http.server 4173` from the repository root and open `/onboarding-demo/`.
